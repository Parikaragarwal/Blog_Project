<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./partials/head') %>
  <title>HomePage</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(120deg, #0f172a, #1e293b);
      color: #f1f5f9;
      line-height: 1.6;
    }

    h1 {
      text-align: center;
      font-size: 3rem;
      font-weight: bold;
      color: #38bdf8;
      margin-bottom: 2rem;
      margin-top: 20px;
      text-transform: uppercase;
      text-shadow: 0 4px 8px rgba(56, 189, 248, 0.5);
    }

    .container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 25px rgba(56, 189, 248, 0.5);
    }

    .card-img-top {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .card-body {
      padding: 1rem;
      color: #e2e8f0;
      text-align: center;
    }

    .card-title {
      font-size: 1.4rem;
      font-weight: bold;
      margin-bottom: 0.5rem;
      color: #38bdf8;
      text-shadow: 0 2px 4px rgba(56, 189, 248, 0.4);
    }

    .btn {
      display: inline-block;
      text-decoration: none;
      background: #38bdf8;
      color: #fff;
      padding: 0.5rem 1rem;
      border-radius: 5px;
      font-weight: bold;
      transition: background 0.3s ease, transform 0.3s ease;
    }

    .btn:hover {
      background: #2563eb;
      transform: translateY(-3px);
    }


  </style>
</head>

<body>
  <%- include('./partials/nav') %>
  <h1>Welcome to Characters' Chronicles</h1>
  <div class="container">
    <% blogs.forEach(blog => { %>
    <div class="card">
      <img src="<%= blog.coverImgUrl %>" class="card-img-top">
      <div class="card-body">
        <h5 class="card-title"><%= blog.title %></h5>
        <a href="/blog/<%= blog.id %>" class="btn">View Blog</a>
      </div>
    </div>
    <% }) %>
  </div>
 
</body>

</html>